apiVersion: v1
kind: Service
metadata:
  name: inference-control-plane
  namespace: inference-control-plane
spec:
  selector:
    app: inference-control-plane
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
