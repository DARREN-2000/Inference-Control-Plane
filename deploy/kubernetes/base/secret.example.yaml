apiVersion: v1
kind: Secret
metadata:
  name: inference-control-plane-secrets
  namespace: inference-control-plane
type: Opaque
stringData:
  DEFAULT_API_KEY: replace-with-strong-api-key
  LLM_API_KEY: optional-if-openai-compatible
  OTLP_ENDPOINT: optional-otlp-endpoint
