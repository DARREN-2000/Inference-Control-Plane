apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-control-plane.fullname" . }}
  labels:
    app: {{ include "inference-control-plane.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ include "inference-control-plane.name" . }}
